Description: 
.to remove button shake by using shift register.

the ip's interface:
.clk:work clock(can be 25MHz or lower)
.clr:reset signal(active high)
.inp:input key signal(1 bit, shake)
.outp:output key signal(1 bit, stable)


