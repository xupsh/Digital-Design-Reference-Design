Description:
.get the keyboard's key value by PS2 interface

the ip's interface:
.mclk:clock input(100MHz)
.clr:reset signal(active high)
.PS2C:ps2 interface clock signal
.PS2D:ps2 interface data signal
.Value:the key value we finally get(16 bit)
