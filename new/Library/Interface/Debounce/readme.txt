Description:
Debounce IP to remove the glitch

Interface:
.clk: work clock input(25MHz or faster)
.clr: reset signal, when rst is '1', reset
.i :  unstable key's signal input  
.o :  stable key's signal output
